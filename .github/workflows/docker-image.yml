name: Benchmark (Python nogil-3.9.10)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag python-image
    - name: Install
      run: docker run python-image make install
    - name: Run benchmark
      run: docker run python-image python python_nogil_benchmark/simple.py
